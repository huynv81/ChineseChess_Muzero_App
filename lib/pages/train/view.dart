import 'package:flutter/material.dart';

class TrainView extends StatelessWidget {
  const TrainView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: const Text("xdx")));
  }
}
